spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:h2:mem:stockflowdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
    hikari:
      # Maximum pool size: 10 conexiones simultáneas - adecuado para carga media (~50-100 req/min)
      maximum-pool-size: 10
      # Minimum idle: mantener 5 conexiones activas para evitar overhead de creación
      minimum-idle: 5
      # Connection timeout: 30 segundos para obtener conexión del pool
      connection-timeout: 30000
      # Idle timeout: cerrar conexiones inactivas después de 10 minutos
      idle-timeout: 600000
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,info
  endpoint:
    health:
      show-details: always
  health:
    defaults:
      enabled: true

resilience4j:
  circuitbreaker:
    instances:
      alertService:
        registerHealthIndicator: true
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true
        waitDurationInOpenState: 5s
        failureRateThreshold: 50
  retry:
    instances:
      movementService:
        maxAttempts: 3
        waitDuration: 1000
        enableExponentialBackoff: true
        exponentialBackoffMultiplier: 2
  ratelimiter:
    instances:
      movementHistory:
        limitForPeriod: 10
        limitRefreshPeriod: 1s
        timeoutDuration: 0

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.stockflow: DEBUG
    org.springframework.web: INFO
    org.hibernate: INFO